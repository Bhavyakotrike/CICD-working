version: 0.2

phases:
  build:
    commands:
      - echo "Build started"
      - echo "Building CICD WebApp"
      - ls -la
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
